#include <iostream>
#include <vector>
#include <string>
using namespace std;

void CODEASL() {
    cout<<"Welcome!     \n";
    vector<string> lines;
    string line ;
    for (int i = 0; i<1000000; i++) {
        getline(cin, line);
    }

}